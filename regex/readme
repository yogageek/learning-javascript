以下將 JavaScript 正則表達式的核心方法與修飾符（Flags）整理為 Markdown 格式：

-----

## 📖 JavaScript 正則表達式核心規範表

### 1\. 核心操作方法 (Methods)

| 分類 | 方法名稱 | 嚴謹定義 | 輸出類型 |
| :--- | :--- | :--- | :--- |
| **判定性檢索** | `test()` | 驗證字串是否符合模式。 | `Boolean` |
| **詳細性檢索** | `exec()` | 執行深度搜尋，提取匹配內容、位置及捕獲組。 | `Array` / `null` |
| **集合性檢索** | `match()` | 依據模式檢索字串，其回傳深度與資訊量受 `g` 標記影響。 | `Array` / `null` |
| **定位性檢索** | `search()` | 檢索與模式匹配的子字串起始位置。 | `Number` (索引值) |

-----

### 2\. 修飾符 (Flags)

| 分類 | 標記 (Flag) | 嚴謹定義 |
| :--- | :--- | :--- |
| **範圍與敏感度控制** | `i`, `g`, `m` | **i (Ignore Case)**：忽略字元大小寫差異。<br>**g (Global)**：全域檢索，不於首位匹配後停止。<br>**m (Multiline)**：多行模式，影響 `^` 與 `$` 的邊界判定。 |
| **字元解析控制** | `s`, `u` | **s (dotAll)**：允許 `.` 匹配包含換行符在內的任意字元。<br>**u (Unicode)**：將字串視為 UTF-16 序列，正確處理 4 位元組字元。 |
| **起始位置控制** | `y` | **y (Sticky)**：粘性匹配，強制從 `lastIndex` 指定的索引位置起始且不跳過字元。 |

-----

### 3\. 方法與修飾符交互邏輯

> **備註：**
>
>   * 使用 `exec()` 或 `match()`（配合 `g`）時，正規表達式對象會維護 `lastIndex` 屬性，記錄下一次匹配的起始點。
>   * `search()` 方法不支援全域檢索，即便標註 `g` 標記，其回傳結果始終為第一個匹配項的索引。



## 1. 建立方式

JavaScript 提供兩種方式定義正則表達式：

* **字面量 (Literal):** 使用 `/pattern/flags`。編譯時載入，效能較佳。
* **構造函數 (Constructor):** 使用 `new RegExp('pattern', 'flags')`。運行時載入，適用於動態生成的模式。

---

## 2. 修飾符 (Flags)

| 標記 | 說明 |
| :--- | :--- |
| **g** | 全域匹配 (Global) |
| **i** | 不區分大小寫 (Ignore case) |
| **m** | 多行匹配 (Multiline) |
| **s** | 允許 `.` 匹配換行符 (DotAll) |
| **u** | 使用 Unicode 模式 |
| **y** | 粘性匹配 (Sticky)，從 `lastIndex` 位置開始匹配 |

---

## 3. 語法結構

### 字元類別 (Character Classes)
* `.`: 匹配除換行符外的任意單一字元。
* `\d`: 匹配數字，等同於 `[0-9]`。
* `\D`: 匹配非數字。
* `\w`: 匹配字母、數字或下劃線，等同於 `[A-Za-z0-9_]`。
* `\W`: 匹配非單字字元。
* `\s`: 匹配空白字元（空白、製表、換頁）。
* `\S`: 匹配非空白字元。

### 量詞 (Quantifiers)
* `*`: 0 次或多次。
* `+`: 1 次或多次。
* `?`: 0 次或 1 次（可選）。
* `{n}`: 剛好 n 次。
* `{n,}`: 至少 n 次。
* `{n,m}`: n 到 m 次。

### 邊界斷言 (Assertions)
* `^`: 匹配字串開頭。
* `$`: 匹配字串結尾。
* `\b`: 匹配單詞邊界。
* `(?=p)`: 正向先行斷言，後方需符合 p。
* `(?!p)`: 負向先行斷言，後方需不符合 p。

---

## 4. 常用方法

### RegExp 對象方法
* **`test(string)`**: 返回布林值，檢測字串是否符合模式。
* **`exec(string)`**: 返回一個數組（包含匹配結果及捕獲組），若無匹配則返回 `null`。

### String 對象方法
* **`match(regexp)`**: 返回匹配結果數組。
* **`matchAll(regexp)`**: 返回包含所有匹配結果（含捕獲組）的迭代器。
* **`replace(regexp, replacement)`**: 替換符合模式的子字串。
* **`search(regexp)`**: 返回第一個匹配項的索引，無則返回 -1。
* **`split(regexp)`**: 使用正則作為分隔符拆分字串。

---

## 5. 捕獲組與邏輯運算

* `(x)`: 捕獲組，匹配 x 並記錄結果。
* `(?:x)`: 非捕獲組，僅匹配 x 但不記錄結果。
* `x|y`: 邏輯「或」，匹配 x 或 y。
* `[abc]`: 字符集合，匹配 a、b 或 c 中任一字符。
* `[^abc]`: 否定字符集合，匹配非 a、b、c 的字元。

您目前是否有具體的字串處理需求（如驗證 Email、擷取特定標籤內容），需要針對性地撰寫正則表達式？



以下將您提供的正規表達式符號規範整理為 Markdown 表格格式：

---

## 🛠 正規表達式模式 (Pattern) 規格表

| 符號 | 說明 | 範例邏輯 |
| :--- | :--- | :--- |
| **`.` (點號)** | 匹配除換行符外的任一字元。 | `.T.` 代表中間為 T，左右各一字元的字串。 |
| **`*` (星號)** | 前一個字元出現 **0 次或多次**。 | `/QO*/` 可匹配 Q, QO, QOO 等。 |
| **`[]` (中括號)** | **字元集合**，匹配括號內的任一字元。 | `[Ee]` 代表匹配大寫 E 或小寫 e。 |
| **`[^]` (否定)** | **排除集合**，匹配不包含在括號內的字元。 | `[^A-Z]` 搜尋任何不是大寫字母的字元。 |
| **`^` (開頭)** | 限制匹配必須在**字串起始處**。 | `/^x/` 搜尋以 x 開頭的字串。 |
| **`$` (結束)** | 限制匹配必須在**字串末尾處**。 | `/[0-9]$/` 搜尋以數字結尾的字串。 |
| **`\` (跳脫)** | **轉義字元**，取消特殊符號的正則功能，視為一般文字。 | `\.` 僅匹配字面上的點號字元。 |
| **`{n,m}`** | **重複次數區間**，限定出現次數介於 n 到 m 之間。 | `[a-z]{3,5}` 匹配連續 3 到 5 個小寫字母。 |

---

### 💡 補充說明
* **中括號內的 `^`**：當 `^` 出現在 `[]` 的第一個字元時，代表「非」或「否定」；若出現在一般模式開頭，則代表「行首錨點」。
* **貪婪匹配**：`*` 預設會匹配盡可能多的字元，若要改為非貪婪模式，需在其後加上問號（例如 `*?`）。